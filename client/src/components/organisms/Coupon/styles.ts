import styled from 'styled-components'

export const boxCoupon = styled.div`
    width: 100%;
    padding: 2px;
    align-items: right;
    position: relative;

    input {
        margin: 0 0 7px 0;
        width: 100%;
    }

  
`

export const boxButton = styled.div`
    width: 50%;
    position: relative;
    display: flex;

    button {
        padding: 10px;
        font-size: 14px;
        margin: 0 0 40px 0;
        cursor: pointer;
    }
`

export const boxPrice = styled.div`
   
   
    h2 {
        text-align: right;
        color: #02732A;
        
    }
`
