{
  "name": "fullstack-domhotel",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npm-run-all --parallel start:client start:server",
    "start:client": "npm start --w=client",
    "start:server": "npm start --w=server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
